
  <div><%= image_tag @group.get_image, size:'100x100' %></div>
  <div><%= @group.name %></div>
	  <div><%= @group.body %></div>
<h4>参加者人数　<%= @participant_size %></h4>
<h4>グループ主　<%= @user.nickname if @user %></h4>

<div class='row'>
  <% if @user.present? %>
    <%= link_to edit_group_path(@group.id), class: "btn btn-outline-secondary btn-block edit_user_#{@user.id}" do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
  <% end %>
</div>
<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>ユーザー情報</h2>
      <%= render 'public/users/info', user: @user_self %>
      <h2 class="mt-3"></h2>
      <%= render 'public/music_posts/form', music_post: @music_post %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>グループ一覧</h2>
      <%= render 'index', groups: @groups %>
    </div>
  </div>
</div>
</div>