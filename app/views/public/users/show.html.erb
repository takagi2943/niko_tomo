<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <%= render 'info', user: @user %>
      二胡の情報を表示させる
    <% if @niko.condition_met? %> 
      <h4>My二胡</h4>
      <%= niko.name %>
      
    <% else %>
      <!-- 条件が満たされていない場合の表示 -->
    <% end %>

      <h2 class="mt-3">My二胡</h2>
      <%#= render 'books/form', book: @book %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Books</h2>
      <%#= render 'books/index',books: @books %>
    </div>
  </div>
</div