<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h4>会員情報</h4>
      <%= render 'public/users/info', user: current_user %>
      <h4 class="mt-3">新規投稿</h4>
      <%= render 'form', music_post: @new_music_post %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <%= render 'public/music_posts/index', music_posts: @music_posts %>
    </div>
    <% if @music_post.user == current_user %>
      <td><%= link_to 'Edit', edit_music_post_path(@music_post), class: "btn btn-sm btn-success" %></td>
      <td><%= link_to 'Destroy', @music_post, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{@music_post.id}"%></td>
    <% end %>
        </tr>
      </table>

     <div id="comment_counter">
      <%= render "public/music_post_comments/counter", music_post_comments: @music_post_comments %>
    </div>
    </div>
